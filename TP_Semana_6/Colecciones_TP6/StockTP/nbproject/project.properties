annotation.processing.enabled=true
build.dir=build
build.generated.dir=${build.dir}/generated
build.classes.dir=${build.dir}/classes
dist.dir=dist
dist.jar=${dist.dir}/StockTP.jar
javac.source=1.8
javac.target=1.8
manifest.file=manifest.mf
platform.active=default_platform
run.classpath=${build.classes.dir}
main.class=stock.MainStock
src.dir=src

# Added to fix Ant: Must set dist.javadoc.dir
dist.javadoc.dir=${dist.dir}/javadoc
javadoc.additionalparam=
source.encoding=UTF-8
includes=**
excludes=
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
test.src.dir=test

# Fix Ant: Must set build.classes.excludes
build.classes.excludes=**/*.java,**/*.form,**/.nbintdb
